buildscript {
  dependencies {
    classpath deps.sqldelight.gradle
  }
}

apply plugin: 'kotlin'
apply plugin: 'com.squareup.sqldelight'

dependencies {
  api project(':modules:aux:sqldelight-flow')
}

sqldelight {
  AlterDatabase {
    packageName = 'de.hannesstruss.alter.db'
  }
}
